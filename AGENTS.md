Source-of-truth monorepo for bob (Bugyi's Obsidian) plugins. Deployed to the vault (in the ~/bob/ directory) via the
`bob plugins sync` command. After making any changes to files in this repo, you MUST run that command to sync your
changes to the vault.
